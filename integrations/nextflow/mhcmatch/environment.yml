channels:
  - conda-forge
dependencies:
  - python >=3.10
  - pip
  - pip:
      # mhcmatch pulls its seqtree C++ core (ARM/x86_64 wheels on PyPI) as a dependency -- no extra.
      - mhcmatch==0.4.1
